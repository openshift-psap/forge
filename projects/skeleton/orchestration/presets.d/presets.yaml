__multiple: true

collect_cluster_info:
  skeleton.collect_cluster_info: true

side_testing:
  skeleton.extra_properties:
    load_shape: round
    request_type: text_completions

quick_test:
  skeleton.test.duration_seconds: 10  # Quick 10 second test

medium_test:
  skeleton.test.duration_seconds: 120  # Medium 2 minute test

long_test:
  skeleton.test.duration_seconds: 300  # Long 5 minute test

agent:
  agentic_review.enabled: true

fail:
  skeleton.test.fail: true
